export type SiteConfig = {
  name: string;
  description: string;
  url: string;
};

export type Profile = {
  id: string;
  full_name: string;
  institution: string;
  subject: string;
  experience_years: number;
  location: string;
  bio: string;
  avatar_url: string;
  created_at: string;
};
